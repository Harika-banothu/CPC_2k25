//find the next higher permutation of the given number as an array of digits. If such a number
//doesn’t exist, return -1.
class Solution {
public:
    void nextPermutation(vector<int>& nums) {
        next_permutation(nums.begin(),nums.end());
       
    }
};
